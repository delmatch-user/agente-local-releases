import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miacardapio.agent',
  appName: 'MiaCardapio Agent Tablet',
  webDir: 'dist'
};

export default config;
